<div data-ng-if="model.formActiveView == 'blocks'" class="row" id="show_blocks">
    <div data-ng-if="model.createAction == true" style="display:block;text-align: center; color:#93a2a9; padding: 20px;">
        Primero debe de <strong>Guardar</strong> para poder agregar Bloques a esta Espectáculo.
    </div>
    <div data-ng-if="model.createAction == false && model.selectedShow.id != undefined">
        <block-selector elementid="model.selectedShow.id" elementtype="show"></block-selector>
    </div>
</div>