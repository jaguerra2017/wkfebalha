<div data-ng-if="model.formActiveView == 'blocks'" class="row" id="page_blocks">
    <div data-ng-if="model.createAction == true" style="display:block;text-align: center; color:#93a2a9; padding: 20px;">
        Primero debe de <strong>Guardar</strong> para poder agregar Bloques a esta Página.
    </div>
    <div data-ng-if="model.createAction == false && model.selectedPage.id != undefined">
        <block-selector elementid="model.selectedPage.id" elementtype="page"></block-selector>
    </div>
</div>