<div data-ng-if="model.formActiveView == 'blocks'" class="row" id="event_blocks">
    <div data-ng-if="model.createAction == true" style="display:block;text-align: center; color:#93a2a9; padding: 20px;">
        Primero debe de <strong>Guardar</strong> para poder agregar Bloques a este Evento.
    </div>
    <div data-ng-if="model.createAction == false && model.selectedEvent.id != undefined">
        <block-selector elementid="model.selectedEvent.id" elementtype="event"></block-selector>
    </div>
</div>